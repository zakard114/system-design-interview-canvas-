{
	# Set TLS_EMAIL in .env when SITE_ADDRESS is a real domain (Let's Encrypt).
	email {$TLS_EMAIL}
}

{$SITE_ADDRESS} {
	# Same origin for UI + API + WebSocket (no CORS drama).
	reverse_proxy app:8000
}
